type CourseMeta = {
	course: string;
	type: string;
	languages: string[];
	level: string;
	order: string[];
	title: string;
	description: string;
	minutes: number; // in minutes
};

export type { CourseMeta };
